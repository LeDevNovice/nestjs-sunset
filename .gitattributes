# Normalize line endings to LF for all text files, regardless of the
# contributor's OS or local `core.autocrlf` git config. Keeps Prettier's
# `--write` output stable and avoids full-repo diff noise on Windows.
* text=auto eol=lf

